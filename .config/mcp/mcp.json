{
  "mcpServers": {
    "google-search": {
      "command": "npx",
      "args": [
        "-y",
        "google-search-api-mcp"
      ],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}",
        "GOOGLE_SEARCH_MODEL": "gemini-2.5-flash"
      }
    }
  }
}
